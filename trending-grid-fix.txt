          {/* Grid ที่ทุก Card เท่ากัน */}
          <div className="grid grid-cols-1 lg:grid-cols-2 gap-6">
            {trendingCamps.map((camp) => (
              <CampCard key={camp.id} camp={camp} variant="detailed" />
            ))}
          </div>
